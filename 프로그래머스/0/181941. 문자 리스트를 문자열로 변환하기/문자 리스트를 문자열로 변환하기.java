class Solution {
    public String solution(String[] arr) {
        StringBuilder st =new StringBuilder();
        
        for(int i=0;i<arr.length;i++)
        {
            st.append(arr[i]);
        }
       
     return st.toString();  
    }
}
